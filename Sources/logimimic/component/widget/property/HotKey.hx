package logimimic.component.widget.property;

import crovown.Crovown;
import crovown.component.widget.Widget;

@:build(crovown.Macro.component())
class HotKey extends Widget {
    public static function build(crow:Crovown, component:HotKey) {
        return component;
    }
}